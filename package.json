{
	"name": "hex-cartographer",
	"version": "1.1.0",
	"description": "A visual hex map editor for Obsidian. Create fantasy world maps with custom SVG symbols, terrain, rivers, roads, and text annotations.",
	"main": "main.js",
	"private": true,
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"test": "vitest run"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"hex",
		"map",
		"rpg"
	],
	"author": "Christoph Werner",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.20.0",
		"obsidian": "latest",
		"tslib": "^2.6.2",
		"typescript": "^5.4.0",
		"vitest": "^4.1.10"
	},
	"allowScripts": {
		"esbuild@0.20.2": true
	}
}
