import { SidebarSection } from "./SidebarSection";

export class IconsSidebarSection extends SidebarSection {
    getId()    { return 'icons'; }
    getLabel() { return 'Icons'; }

    protected build(): void {
        // TODO: populate icon controls
    }
}
